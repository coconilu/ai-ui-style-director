name: Catalog Pages

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: catalog-pages-${{ github.event_name == 'pull_request' && github.event.pull_request.number || 'production' }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 20
      - name: Install dependencies
        run: npm ci
      - name: Build static catalog
        run: npm run catalog:build
      - name: Validate Pages artifact
        shell: bash
        run: |
          test -f dist/pages/index.html
          test -f dist/pages/catalog.json
          test -f dist/pages/app.js
          test -f dist/pages/styles.css
          test -f dist/pages/.nojekyll
          direction_count="$(node -p "require('./dist/pages/catalog.json').directionCount")"
          style_count="$(node -p "require('./dist/pages/catalog.json').styleCount")"
          expected_v2="$(node -p "require('./dist/pages/catalog.json').linkCount")"
          expected_legacy="$(node -p "require('./dist/pages/catalog.json').aliases.length")"
          actual_v2="$(find dist/pages/previews/v2 -type f -name '*.svg' | wc -l)"
          actual_legacy="$(find dist/pages/previews -maxdepth 1 -type f -name '*.svg' | wc -l)"
          test "$style_count" -eq "$direction_count"
          test "$actual_v2" -eq "$expected_v2"
          test "$actual_legacy" -eq "$expected_legacy"
      - name: Upload Pages artifact
        if: github.event_name != 'pull_request'
        uses: actions/upload-pages-artifact@v5
        with:
          path: dist/pages

  deploy:
    if: github.event_name != 'pull_request' && github.ref == 'refs/heads/main'
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Configure Pages
        uses: actions/configure-pages@v6
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
